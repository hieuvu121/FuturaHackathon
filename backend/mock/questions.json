[
  {"id": "q-001", "type": "recall", "target": {"file": "app/billing/invoice.py", "lines": [41, 132], "commit": "a1b2c3d4"}, "target_name": "process_invoice", "prompt": "You wrote `process_invoice` in `app/billing/invoice.py` two months ago. Without reading it closely: what does it do, and what inputs and outputs did you design it around?", "code_context": "def process_invoice(payload, customer, *, dry_run=False):\n    items = validate_line_items(payload['items'])\n    subtotal = sum(i['amount'] for i in items)\n    tax = apply_tax(subtotal, customer.jurisdiction)\n    ...", "skill_ids": ["python", "rest_api_design"]},
  {"id": "q-002", "type": "justify", "target": {"file": "app/billing/tax.py", "lines": [12, 58], "commit": "e5f6a7b8"}, "target_name": "apply_tax", "prompt": "`apply_tax` branches on the jurisdiction string in five separate places. What were you trading off against a table- or strategy-driven approach, and what would have broken with the simpler version?", "code_context": "def apply_tax(subtotal, jurisdiction):\n    if jurisdiction == 'AU':\n        ...\n    elif jurisdiction == 'NZ':\n        ...", "skill_ids": ["python"]},
  {"id": "q-003", "type": "transfer", "target": {"file": "app/storage/repository.py", "lines": [88, 141], "commit": "778899aa"}, "target_name": "persist_invoice", "prompt": "Suppose `persist_invoice` had to run concurrently across ten workers against the same database. What breaks first, and what would you change?", "code_context": "def persist_invoice(invoice):\n    conn = get_connection()\n    try:\n        ...\n    except Exception:\n        return None", "skill_ids": ["sql", "async_programming"]},
  {"id": "q-004", "type": "debug", "target": {"file": "app/billing/invoice.py", "lines": [12, 39], "commit": "c9d0e1f2"}, "target_name": "validate_line_items", "prompt": "A bug has been introduced into `validate_line_items`. One test in the suite now fails. Find it and fix it.", "code_context": "def validate_line_items(items):\n    valid = []\n    for item in items:\n        if item['amount'] > 0:\n            valid.append(item)\n    return valid", "skill_ids": ["python", "testing_unit"]},
  {"id": "q-005", "type": "extend", "target": {"file": "app/billing/tax.py", "lines": [60, 74], "commit": "33445566"}, "target_name": "lookup_rate", "prompt": "Add support for a date-effective rate to `lookup_rate` -- the same jurisdiction can have different rates before and after a cutover date -- without breaking the existing tests.", "code_context": "def lookup_rate(jurisdiction):\n    return RATES[jurisdiction]", "skill_ids": ["python", "testing_unit"]}
]
